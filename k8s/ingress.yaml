apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: citizen-service-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: citizen-service.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: citizen-service
                port:
                  number: 80
